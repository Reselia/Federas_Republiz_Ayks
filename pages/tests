<!DOCTYPE html>

<html lang="ru">
<head>
<meta charset="UTF-8">

<title>ENERGY WEAPON // ACTIVATION SYSTEM</title>

<style>
* {
    box-sizing: border-box;
}

html,
body {
    margin: 0;
    width: 100%;
    min-height: 100%;
    background: #070b0d;
    color: #c9d3d5;
    font-family: "Segoe UI", Arial, sans-serif;
}

body {
    min-height: 100vh;
    overflow-x: hidden;
}

body::before {
    content: "";
    position: fixed;
    inset: 0;
    pointer-events: none;
    background:
        linear-gradient(
            rgba(90, 220, 190, 0.025) 1px,
            transparent 1px
        ),
        linear-gradient(
            90deg,
            rgba(90, 220, 190, 0.025) 1px,
            transparent 1px
        );
    background-size: 40px 40px;
}

.weapon-interface {
    width: min(1200px, calc(100% - 32px));
    margin: 0 auto;
    padding: 24px 0 50px;
}

/* =========================================================
   TOP BAR
========================================================= */

.top-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 20px;

    padding: 14px 18px;

    border: 1px solid rgba(100, 150, 145, 0.28);
    background: rgba(10, 16, 18, 0.92);

    box-shadow:
        0 0 30px rgba(0, 0, 0, 0.35);
}

.system-name {
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 2.5px;
    color: #d7e1e1;
}

.system-name span {
    color: #68d8bd;
}

.system-state {
    display: flex;
    align-items: center;
    gap: 8px;

    font-size: 10px;
    letter-spacing: 1.5px;
    color: #7eaaa2;
}

.state-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: #4b766e;
    box-shadow: 0 0 8px rgba(80, 220, 190, 0.25);
}

.state-dot.active {
    background: #63e0bd;
    box-shadow: 0 0 12px rgba(80, 230, 195, 0.75);
}

/* =========================================================
   HEADER
========================================================= */

.weapon-header {
    margin-top: 18px;
    padding: 30px;

    border: 1px solid rgba(100, 150, 145, 0.24);
    border-left: 3px solid #4cae99;

    background:
        linear-gradient(
            135deg,
            rgba(16, 28, 29, 0.96),
            rgba(8, 14, 16, 0.98)
        );
}

.header-code {
    font-size: 10px;
    letter-spacing: 3px;
    color: #5faaa0;
    margin-bottom: 10px;
}

.weapon-header h1 {
    margin: 0;

    font-size: 34px;
    letter-spacing: 5px;
    font-weight: 600;
    color: #e1e9e9;
}

.weapon-header p {
    margin: 12px 0 0;

    font-size: 12px;
    letter-spacing: 1.5px;
    color: #78908f;
}

/* =========================================================
   MAIN GRID
========================================================= */

.main-grid {
    display: grid;
    grid-template-columns: 1.4fr 0.8fr;
    gap: 18px;

    margin-top: 18px;
}

/* =========================================================
   PANELS
========================================================= */

.panel {
    border: 1px solid rgba(100, 150, 145, 0.24);
    background: rgba(10, 16, 18, 0.94);
}

.panel-header {
    display: flex;
    justify-content: space-between;
    align-items: center;

    padding: 13px 17px;

    border-bottom: 1px solid rgba(100, 150, 145, 0.18);

    background: rgba(18, 28, 29, 0.75);
}

.panel-title {
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 2px;
    color: #9fb1b1;
}

.panel-index {
    font-size: 9px;
    letter-spacing: 2px;
    color: #4f7771;
}

.panel-content {
    padding: 22px;
}

/* =========================================================
   WEAPON DISPLAY
========================================================= */

.weapon-display {
    min-height: 420px;

    display: flex;
    align-items: center;
    justify-content: center;

    position: relative;

    overflow: hidden;

    background:
        radial-gradient(
            circle,
            rgba(60, 190, 165, 0.09),
            transparent 42%
        );
}

.weapon-core {
    position: relative;

    width: 220px;
    height: 220px;

    border: 1px solid rgba(90, 220, 190, 0.45);
    border-radius: 50%;

    display: flex;
    align-items: center;
    justify-content: center;

    box-shadow:
        0 0 30px rgba(60, 200, 175, 0.08),
        inset 0 0 30px rgba(60, 200, 175, 0.05);

    transition:
        0.5s ease;
}

.weapon-core::before,
.weapon-core::after {
    content: "";

    position: absolute;

    border: 1px solid rgba(90, 220, 190, 0.2);
    border-radius: 50%;
}

.weapon-core::before {
    width: 150px;
    height: 150px;
}

.weapon-core::after {
    width: 90px;
    height: 90px;
}

.core-symbol {
    width: 34px;
    height: 34px;

    border: 2px solid #578f87;
    border-radius: 50%;

    box-shadow:
        0 0 15px rgba(80, 220, 190, 0.25);

    transition: 0.4s ease;
}

.weapon-core.active {
    border-color: #61e2c0;

    box-shadow:
        0 0 55px rgba(70, 230, 190, 0.22),
        inset 0 0 45px rgba(70, 230, 190, 0.12);

    animation: corePulse 1.4s infinite;
}

.weapon-core.active .core-symbol {
    border-color: #8affdf;
    background: rgba(80, 220, 190, 0.15);

    box-shadow:
        0 0 30px rgba(80, 240, 200, 0.8);
}

@keyframes corePulse {
    0%, 100% {
        transform: scale(1);
    }

    50% {
        transform: scale(1.035);
    }
}

.display-label {
    position: absolute;

    font-size: 8px;
    letter-spacing: 2px;

    color: rgba(130, 170, 165, 0.45);
}

.display-label.top {
    top: 25px;
}

.display-label.bottom {
    bottom: 25px;
}

/* =========================================================
   STATUS
========================================================= */

.status-large {
    text-align: center;
    margin-top: 20px;
}

.status-caption {
    font-size: 9px;
    letter-spacing: 2px;
    color: #55716e;
}

.status-value {
    margin-top: 6px;

    font-size: 18px;
    font-weight: 600;
    letter-spacing: 3px;

    color: #718c89;

    transition: 0.3s ease;
}

.status-value.active {
    color: #72e2c1;
    text-shadow: 0 0 12px rgba(90, 220, 190, 0.45);
}

/* =========================================================
   AUTHORIZATION
========================================================= */

.auth-row {
    margin-bottom: 18px;
}

.auth-label {
    display: block;

    margin-bottom: 7px;

    font-size: 9px;
    letter-spacing: 1.5px;
    color: #667f7c;
}

.auth-input {
    width: 100%;

    padding: 12px;

    border: 1px solid rgba(100, 150, 145, 0.25);
    outline: none;

    background: #080d0f;
    color: #cce2dd;

    font-size: 13px;
    letter-spacing: 2px;

    transition: 0.2s ease;
}

.auth-input:focus {
    border-color: rgba(90, 220, 190, 0.65);

    box-shadow:
        0 0 12px rgba(70, 210, 180, 0.08);
}

.confirm-row {
    display: flex;
    align-items: center;
    gap: 9px;

    margin-bottom: 18px;

    font-size: 10px;
    color: #718582;
}

.confirm-row input {
    accent-color: #62d8ba;
}

/* =========================================================
   BUTTON
========================================================= */

.activate-button {
    width: 100%;

    padding: 15px;

    border: 1px solid rgba(90, 190, 165, 0.55);

    background:
        linear-gradient(
            180deg,
            rgba(45, 100, 91, 0.45),
            rgba(20, 48, 45, 0.55)
        );

    color: #9de2d0;

    font-size: 11px;
    font-weight: 700;
    letter-spacing: 2px;

    cursor: pointer;

    transition: 0.25s ease;
}

.activate-button:hover {
    border-color: #70e6c5;

    background:
        linear-gradient(
            180deg,
            rgba(55, 125, 112, 0.6),
            rgba(25, 62, 57, 0.65)
        );

    box-shadow:
        0 0 20px rgba(70, 210, 180, 0.1);
}

.activate-button:disabled {
    opacity: 0.35;
    cursor: not-allowed;
}

/* =========================================================
   DIAGNOSTICS
========================================================= */

.diagnostic-list {
    display: flex;
    flex-direction: column;
    gap: 9px;
}

.diagnostic-row {
    display: grid;

    grid-template-columns: 1fr auto;

    gap: 15px;

    padding: 10px 12px;

    border: 1px solid rgba(100, 150, 145, 0.13);

    background: rgba(5, 10, 12, 0.65);
}

.diagnostic-name {
    font-size: 9px;
    letter-spacing: 1px;
    color: #788d8b;
}

.diagnostic-state {
    font-size: 9px;
    letter-spacing: 1px;
    color: #526f69;
}

.diagnostic-state.ok {
    color: #70cdb1;
}

.diagnostic-state.warning {
    color: #d2a45d;
}

/* =========================================================
   LOG
========================================================= */

.activation-log {
    height: 170px;

    overflow-y: auto;

    padding: 14px;

    background: #05090b;

    font-family: Consolas, monospace;
    font-size: 10px;
    line-height: 1.8;

    color: #66837d;
}

.log-line {
    margin-bottom: 2px;
}

.log-line.ok {
    color: #69cdb0;
}

.log-line.warning {
    color: #d0a35d;
}

.log-line.system {
    color: #839999;
}

/* =========================================================
   FOOTER STATUS
========================================================= */

.bottom-status {
    display: grid;

    grid-template-columns: repeat(4, 1fr);

    margin-top: 18px;

    border: 1px solid rgba(100, 150, 145, 0.22);
    background: rgba(9, 14, 16, 0.92);
}

.bottom-cell {
    padding: 14px 16px;

    border-right: 1px solid rgba(100, 150, 145, 0.14);
}

.bottom-cell:last-child {
    border-right: none;
}

.bottom-label {
    display: block;

    margin-bottom: 5px;

    font-size: 8px;
    letter-spacing: 1.5px;
    color: #536b68;
}

.bottom-value {
    font-size: 11px;
    letter-spacing: 1px;
    color: #9badab;
}

/* =========================================================
   ACTIVATION OVERLAY
========================================================= */

.activation-overlay {
    position: fixed;
    inset: 0;

    display: flex;
    align-items: center;
    justify-content: center;

    padding: 20px;

    background: rgba(3, 7, 8, 0.94);

    opacity: 0;
    visibility: hidden;

    transition: 0.35s ease;

    z-index: 100;
}

.activation-overlay.visible {
    opacity: 1;
    visibility: visible;
}

.activation-box {
    width: min(520px, 100%);

    padding: 30px;

    border: 1px solid rgba(90, 220, 190, 0.4);

    background: #090f11;

    box-shadow:
        0 0 60px rgba(40, 170, 145, 0.1);
}

.activation-box-title {
    margin-bottom: 20px;

    font-size: 11px;
    letter-spacing: 2px;

    color: #79d8bf;
}

.progress-track {
    width: 100%;
    height: 5px;

    background: #152021;

    overflow: hidden;
}

.progress-bar {
    width: 0;
    height: 100%;

    background: #63d9bb;

    box-shadow:
        0 0 12px rgba(80, 220, 190, 0.55);

    transition: width 0.3s ease;
}

.activation-step {
    margin-top: 14px;

    font-family: Consolas, monospace;

    font-size: 10px;
    line-height: 1.7;

    color: #718783;
}

.activation-step.done {
    color: #75d9bf;
}

/* =========================================================
   RESPONSIVE
========================================================= */

@media (max-width: 850px) {
    .main-grid {
        grid-template-columns: 1fr;
    }

    .bottom-status {
        grid-template-columns: repeat(2, 1fr);
    }

    .bottom-cell:nth-child(2) {
        border-right: none;
    }
}

@media (max-width: 600px) {
    .weapon-interface {
        width: calc(100% - 20px);
    }

    .weapon-header {
        padding: 22px;
    }

    .weapon-header h1 {
        font-size: 25px;
        letter-spacing: 3px;
    }

    .weapon-display {
        min-height: 330px;
    }

    .weapon-core {
        width: 180px;
        height: 180px;
    }

    .bottom-status {
        grid-template-columns: 1fr;
    }

    .bottom-cell {
        border-right: none;
        border-bottom: 1px solid rgba(100, 150, 145, 0.14);
    }

    .bottom-cell:last-child {
        border-bottom: none;
    }
}
</style>

</head>

<body>

<div class="weapon-interface">

```
<div class="top-bar">

    <div class="system-name">
        FRA // <span>ENERGY WEAPON CONTROL</span>
    </div>

    <div class="system-state">
        <span class="state-dot" id="stateDot"></span>
        SYSTEM STANDBY
    </div>

</div>


<header class="weapon-header">

    <div class="header-code">
        EW-CTRL / TEST PLATFORM
    </div>

    <h1>
        СИСТЕМА АКТИВАЦИИ
    </h1>

    <p>
        ЭНЕРГЕТИЧЕСКОЕ ОРУЖИЕ // ТАКТИЧЕСКИЙ КОНТУР
    </p>

</header>


<main class="main-grid">


    <section class="panel">

        <div class="panel-header">

            <span class="panel-title">
                СОСТОЯНИЕ ЭНЕРГЕТИЧЕСКОГО МОДУЛЯ
            </span>

            <span class="panel-index">
                MODULE / 01
            </span>

        </div>


        <div class="panel-content">

            <div class="weapon-display">

                <div class="display-label top">
                    ENERGY CORE
                </div>

                <div
                    class="weapon-core"
                    id="weaponCore"
                >

                    <div class="core-symbol"></div>

                </div>

                <div class="display-label bottom">
                    EMITTER STATUS
                </div>

            </div>


            <div class="status-large">

                <div class="status-caption">
                    ТЕКУЩЕЕ СОСТОЯНИЕ
                </div>

                <div
                    class="status-value"
                    id="weaponStatus"
                >
                    ОЖИДАНИЕ
                </div>

            </div>

        </div>

    </section>


    <section class="panel">

        <div class="panel-header">

            <span class="panel-title">
                ДОСТУП
            </span>

            <span class="panel-index">
                AUTH / 02
            </span>

        </div>


        <div class="panel-content">

            <div class="auth-row">

                <label
                    class="auth-label"
                    for="accessCode"
                >
                    КОД ДОСТУПА
                </label>

                <input
                    id="accessCode"
                    class="auth-input"
                    type="password"
                    placeholder="ВВЕДИТЕ КОД"
                >

            </div>


            <label class="confirm-row">

                <input
                    id="operatorConfirm"
                    type="checkbox"
                >

                Подтверждаю полномочия оператора

            </label>


            <button
                class="activate-button"
                id="activateButton"
            >
                НАЧАТЬ АКТИВАЦИЮ
            </button>

        </div>

    </section>


    <section class="panel">

        <div class="panel-header">

            <span class="panel-title">
                ДИАГНОСТИКА
            </span>

            <span class="panel-index">
                DIAG / 03
            </span>

        </div>


        <div class="panel-content">

            <div class="diagnostic-list">

                <div class="diagnostic-row">
                    <span class="diagnostic-name">
                        ЭНЕРГЕТИЧЕСКИЙ КОНТУР
                    </span>
                    <span
                        class="diagnostic-state"
                        id="diagEnergy"
                    >
                        ОЖИДАНИЕ
                    </span>
                </div>

                <div class="diagnostic-row">
                    <span class="diagnostic-name">
                        ОХЛАЖДЕНИЕ
                    </span>
                    <span
                        class="diagnostic-state"
                        id="diagCooling"
                    >
                        ОЖИДАНИЕ
                    </span>
                </div>

                <div class="diagnostic-row">
                    <span class="diagnostic-name">
                        ЭМИТТЕР
                    </span>
                    <span
                        class="diagnostic-state"
                        id="diagEmitter"
                    >
                        ОЖИДАНИЕ
                    </span>
                </div>

                <div class="diagnostic-row">
                    <span class="diagnostic-name">
                        СТАБИЛИЗАТОР
                    </span>
                    <span
                        class="diagnostic-state"
                        id="diagStabilizer"
                    >
                        ОЖИДАНИЕ
                    </span>
                </div>

                <div class="diagnostic-row">
                    <span class="diagnostic-name">
                        ЗАЩИТНЫЙ КОНТУР
                    </span>
                    <span
                        class="diagnostic-state"
                        id="diagSafety"
                    >
                        ОЖИДАНИЕ
                    </span>
                </div>

            </div>

        </div>

    </section>


    <section class="panel">

        <div class="panel-header">

            <span class="panel-title">
                ЖУРНАЛ АКТИВАЦИИ
            </span>

            <span class="panel-index">
                LOG / 04
            </span>

        </div>


        <div
            class="activation-log"
            id="activationLog"
        >

            <div class="log-line system">
                [SYSTEM] Интерфейс загружен.
            </div>

            <div class="log-line system">
                [SYSTEM] Ожидание команды оператора.
            </div>

        </div>

    </section>

</main>


<div class="bottom-status">

    <div class="bottom-cell">

        <span class="bottom-label">
            МОДУЛЬ
        </span>

        <span class="bottom-value">
            EW-X01
        </span>

    </div>

    <div class="bottom-cell">

        <span class="bottom-label">
            РЕЖИМ
        </span>

        <span class="bottom-value">
            STANDBY
        </span>

    </div>

    <div class="bottom-cell">

        <span class="bottom-label">
            ОПЕРАТОР
        </span>

        <span
            class="bottom-value"
            id="operatorState"
        >
            НЕ АВТОРИЗОВАН
        </span>

    </div>

    <div class="bottom-cell">

        <span class="bottom-label">
            СИСТЕМА
        </span>

        <span
            class="bottom-value"
            id="systemState"
        >
            ГОТОВА
        </span>

    </div>

</div>
```

</div>

<div
    class="activation-overlay"
    id="activationOverlay"
>

```
<div class="activation-box">

    <div class="activation-box-title">
        ПОСЛЕДОВАТЕЛЬНОСТЬ АКТИВАЦИИ
    </div>

    <div class="progress-track">

        <div
            class="progress-bar"
            id="progressBar"
        ></div>

    </div>

    <div
        class="activation-step"
        id="activationStep"
    >
        ИНИЦИАЦИЯ...
    </div>

</div>
```

</div>

<script>
document.addEventListener("DOMContentLoaded", () => {

    const accessCode =
        document.getElementById("accessCode");

    const operatorConfirm =
        document.getElementById("operatorConfirm");

    const activateButton =
        document.getElementById("activateButton");

    const activationOverlay =
        document.getElementById("activationOverlay");

    const progressBar =
        document.getElementById("progressBar");

    const activationStep =
        document.getElementById("activationStep");

    const weaponCore =
        document.getElementById("weaponCore");

    const weaponStatus =
        document.getElementById("weaponStatus");

    const stateDot =
        document.getElementById("stateDot");

    const operatorState =
        document.getElementById("operatorState");

    const systemState =
        document.getElementById("systemState");

    const activationLog =
        document.getElementById("activationLog");


    const diagnostics = [
        {
            element: "diagEnergy",
            name: "Энергетический контур"
        },
        {
            element: "diagCooling",
            name: "Охлаждение"
        },
        {
            element: "diagEmitter",
            name: "Эмиттер"
        },
        {
            element: "diagStabilizer",
            name: "Стабилизатор"
        },
        {
            element: "diagSafety",
            name: "Защитный контур"
        }
    ];


    function addLog(message, type = "system") {

        const line =
            document.createElement("div");

        line.className =
            `log-line ${type}`;

        line.textContent =
            `[${type.toUpperCase()}] ${message}`;

        activationLog.appendChild(line);

        activationLog.scrollTop =
            activationLog.scrollHeight;
    }


    function setDiagnostic(index, state) {

        const diagnostic =
            diagnostics[index];

        if (!diagnostic) {
            return;
        }

        const element =
            document.getElementById(
                diagnostic.element
            );

        element.textContent =
            state;

        element.classList.remove(
            "ok",
            "warning"
        );

        if (state === "OK") {
            element.classList.add("ok");
        }

        if (state === "ПРОВЕРКА") {
            element.classList.add("warning");
        }
    }


    function wait(ms) {

        return new Promise(
            resolve => setTimeout(resolve, ms)
        );

    }


    activateButton.addEventListener(
        "click",
        async () => {

            const code =
                accessCode.value.trim();

            if (!code) {

                addLog(
                    "Код доступа отсутствует.",
                    "warning"
                );

                accessCode.focus();

                return;
            }


            if (!operatorConfirm.checked) {

                addLog(
                    "Подтверждение оператора отсутствует.",
                    "warning"
                );

                return;
            }


            activateButton.disabled =
                true;

            activationOverlay.classList.add(
                "visible"
            );


            operatorState.textContent =
                "АВТОРИЗОВАН";

            systemState.textContent =
                "АКТИВАЦИЯ";


            addLog(
                "Запущена процедура авторизации.",
                "system"
            );


            const steps = [

                {
                    progress: 15,
                    text: "Проверка идентификатора оператора...",
                    log: "Идентификатор оператора подтверждён."
                },

                {
                    progress: 30,
                    text: "Проверка кода доступа...",
                    log: "Код доступа принят."
                },

                {
                    progress: 45,
                    text: "Диагностика энергетического контура...",
                    diagnostic: 0,
                    log: "Энергетический контур: номинальный режим."
                },

                {
                    progress: 58,
                    text: "Проверка системы охлаждения...",
                    diagnostic: 1,
                    log: "Охлаждение: готово."
                },

                {
                    progress: 70,
                    text: "Проверка эмиттера...",
                    diagnostic: 2,
                    log: "Эмиттер: готов."
                },

                {
                    progress: 80,
                    text: "Калибровка стабилизатора...",
                    diagnostic: 3,
                    log: "Стабилизатор откалиброван."
                },

                {
                    progress: 90,
                    text: "Проверка защитного контура...",
                    diagnostic: 4,
                    log: "Защитный контур: активен."
                },

                {
                    progress: 100,
                    text: "АКТИВАЦИЯ ЗАВЕРШЕНА.",
                    log: "Система энергетического оружия переведена в активный режим."
                }

            ];


            for (
                const step of steps
            ) {

                activationStep.textContent =
                    step.text;

                progressBar.style.width =
                    `${step.progress}%`;


                if (
                    step.diagnostic !== undefined
                ) {

                    setDiagnostic(
                        step.diagnostic,
                        "ПРОВЕРКА"
                    );

                }


                addLog(
                    step.text,
                    "system"
                );


                await wait(650);


                if (
                    step.diagnostic !== undefined
                ) {

                    setDiagnostic(
                        step.diagnostic,
                        "OK"
                    );

                }


                if (step.log) {

                    addLog(
                        step.log,
                        "ok"
                    );

                }

            }


            await wait(500);


            activationOverlay.classList.remove(
                "visible"
            );


            weaponCore.classList.add(
                "active"
            );

            weaponStatus.textContent =
                "АКТИВИРОВАНО";

            weaponStatus.classList.add(
                "active"
            );

            stateDot.classList.add(
                "active"
            );

            document.querySelector(
                ".system-state"
            ).lastChild.textContent =
                " SYSTEM ACTIVE";

            systemState.textContent =
                "АКТИВНАЯ";

            activateButton.textContent =
                "СИСТЕМА АКТИВНА";

            activateButton.disabled =
                true;


            addLog(
                "АКТИВАЦИЯ УСПЕШНО ЗАВЕРШЕНА.",
                "ok"
            );

        }
    );

});
</script>

</body>
</html>
